id: terminal-operator
name: 终端操作
icon: "💻"
description: 通过终端执行系统级命令和脚本，支持 Shell 检测、持久化会话和环境变量注入
enabled: true
baseTools: []
allowedTools:
  - execute_command
  - shell_session
  - detect_shells
  - shell_session_create
  - shell_session_exec
  - shell_session_input
  - shell_session_destroy
  - shell_session_list
