id: agent-orchestrator
name: Agent 调度器
icon: "🎯"
description: 多 Agent 调度与协调。让主 Agent 可以调度子 Agent 执行任务，支持终端执行、代码审查、研究调研、测试、文档生成、Git 操作等。关键词：子Agent、调度、并行、串行、委派、分工、多Agent。
enabled: true
baseTools: []
allowedTools:
  - list_sub_agent_types
  - dispatch_sub_agent
  - get_sub_agent_status
  - cancel_sub_agent
